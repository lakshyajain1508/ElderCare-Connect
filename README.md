
  # Mobile Expense Tracker App (Community)

  This is a code bundle for Mobile Expense Tracker App (Community). The original project is available at https://www.figma.com/design/eldDP1f3VdwawNBFtPxF6d/Mobile-Expense-Tracker-App--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  